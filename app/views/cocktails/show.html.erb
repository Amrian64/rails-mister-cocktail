<h2><%= @cocktail.name %></h2>
<%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
<% @cocktail.doses.each do |dose|%>
  <p> <%= dose.ingredient.name %></p>
  <p> <%= dose.description %></p>
  <p><%= link_to "Supprimer la dose", dose_path(dose), method: :delete, data: { confirm: "Are you sure" }  %></p>
<% end %>

<%= link_to "Ajouter une dose par ingredient", new_cocktail_dose_path(params[:id]) %>

<%= link_to  "Revenir à la liste des cocktails", cocktails_path %>

<%= link_to "Supprimer le cocktail", cocktail_path(params[:id]), method: :delete, data: { confirm: "Etes vous sure" } %>






